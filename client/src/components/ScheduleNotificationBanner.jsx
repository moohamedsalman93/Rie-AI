/**
 * Legacy no-op: schedule completion UI moved to ScheduleNotificationsBell.
 * Accepts props so stale JSX still parses; renders nothing.
 */
export function ScheduleNotificationBanner(_props) {
  return null;
}
